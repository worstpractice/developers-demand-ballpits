/**
 * A sufficiently robust string type for *this* particular challenge.
 *
 * Don't just do this and call it a day in production.
 */
export type IpV4Address = `${number}.${number}.${number}.${number}`;
